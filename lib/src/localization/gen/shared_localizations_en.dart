import 'shared_localizations.dart';

/// The translations for English (`en`).
class SharedLocsEn extends SharedLocs {
  SharedLocsEn([String locale = 'en']) : super(locale);

  @override
  String get moduleTitle => 'shared_lib';
}
